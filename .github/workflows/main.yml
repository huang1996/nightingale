name: Build

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
  
env:
  GO_VERSION: 1.23
  DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}

jobs:
  goreleaser:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v4  # 从 v3 升级到 v4
        with:
          fetch-depth: 0
      
      - name: Setup Go Environment
        uses: actions/setup-go@v5  # 从 v3 升级到 v5
        with:
          go-version: ${{ env.GO_VERSION }}
      
      - name: Login to Docker Hub
        uses: docker/login-action@v3  # 从 v2 升级到 v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3  # 从 v2 升级到 v3
      
      - name: Run GoReleaser
        uses: goreleaser/goreleaser-action@v6  # 从 v3 升级到 v6
        with:
          distribution: goreleaser
          version: '~> v2'  # 从 ~> v1 升级到 ~> v2
          args: release --clean
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
          DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
